if (interactive()) {
	library(colorout)
  # Color scheme for dark background
  setOutputColors256(
                     normal = 40,
                     number = 214,
                     string = 85,
                     const = 35,
                     stderror = 45,
                     error = c(1, 0, 1),
                     warn = c(1, 0, 100)
                     )
  # Color scheme for light background
  # setOutputColors256(
                     # normal = c(0, 0, 18), 
                     # number = c(0, 0, 5), 
                     # string = c(0, 0, 22), 
                     # const = c(0, 0, 94),
                     # stderror = 52
                     # )
	library(setwidth)
	options(vimcom.verbose = -1)
	library(vimcom)
}

# suppressMessages(
#                  c(library(ggplot2),
#                    library(dplyr),
#                    library(reshape2))
#                  )
